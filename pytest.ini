[pytest]
asyncio_mode = auto
markers =
    integration: requires live server or external processes
    e2e: end-to-end tests (opt-in)
    perf: performance/load tests (opt-in)
    slow: slow tests (opt-in)
    visual: visual regression tests (opt-in)
